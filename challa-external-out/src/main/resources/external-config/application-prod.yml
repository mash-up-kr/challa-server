s3:
  bucket: ${S3_BUCKET}
  region: ${S3_REGION:ap-northeast-2}
  presigned-url-ttl: ${S3_PRESIGNED_URL_TTL:5m}
  access-key: ${AWS_ACCESS_KEY_ID:}
  secret-key: ${AWS_SECRET_ACCESS_KEY:}
  file-path:
    camera-filters: challa/camera-filters.json
