spring:
  config:
    import:
      - classpath:persistence-config/application-prod.yml

# 인증 계층이 없으므로 prod에서는 API 문서를 노출하지 않는다.
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
